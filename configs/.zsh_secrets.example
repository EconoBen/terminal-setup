#!/bin/zsh
# Sensitive configuration example
# Copy this to ~/.zsh_secrets and customize

# SSH shortcuts
alias myserver='ssh user@my-server.com'
alias pi='ssh pi@raspberry-pi.local'

# Database connections
alias devdb='psql -h localhost -U myuser -d mydb'

# API keys and tokens (never commit these!)
# export OPENAI_API_KEY="your-key-here"
# export AWS_ACCESS_KEY_ID="your-key-here"

# Company-specific aliases
# alias work-vpn='sudo openconnect vpn.company.com'

# Personal/sensitive environment variables
# export PERSONAL_SERVER="10.0.0.1"
# export WORK_VPN_PASSWORD="..."